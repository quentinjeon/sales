# 온라인몰 수익률 관리 시스템

**엑셀 3장을 넣으면 그 달의 「제품별 매출」과 「채널별 매출·이익률」을 보여주는 웹 도구.**

```
📦 01_상품정보.xlsx   ─┐
🛒 02_채널정보.xlsx   ─┼─→ 표준화·매핑 → 손익 계산 → 📊 대시보드 + 📗 결과 엑셀
🧾 03_매출_YYYY-MM.xlsx ─┘
```

총매출은 다들 압니다. **이익을 모릅니다.**
채널마다 수수료가 6.8%~24.0%로 다르고, 상품명이 제각각이라 제품 단위 합산이 안 되고,
원가·물류비가 빠진 숫자로 "잘 팔린다"고 판단하기 때문입니다.

이 도구는 **팔수록 손해인 제품**을 숫자로 드러냅니다.

> ⚠ **데이터는 전부 목업입니다.** 회사·브랜드·제품·채널·금액이 모두 가상이며
> 실존 기업·제품·판매채널과 무관합니다. 손익 구조만 실무와 같게 설계했습니다.

---

## 빠른 시작

```bash
python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
```

```bash
.venv/bin/python -m app.cli init && .venv/bin/python -m app.demo
```

```bash
.venv/bin/python -m uvicorn app.web:app --reload --port 8010
```

→ http://127.0.0.1:8010

화면 기획만 먼저 보려면 브라우저로 [mock/index.html](mock/index.html) 을 여세요.

---

## 문서

| 문서 | 내용 |
|---|---|
| [docs/프로그램-소개.md](docs/프로그램-소개.md) | **화면 6장으로 보는 프로그램 소개** |
| [CLAUDE.md](CLAUDE.md) | 인계 문서 — 개발하려면 먼저 읽으세요 |
| [docs/PRD.md](docs/PRD.md) | 개발 명세. 입력·계산·화면·검수 기준 |
| [docs/목업데이터-소개.md](docs/목업데이터-소개.md) | 상품 DB · 채널 DB · 매출 데이터 · 결과 리포트 |
| [docs/lecture/README.md](docs/lecture/README.md) | 바이브코딩 수업 진행 순서 |
| [docs/lecture/바이브코딩-시작하기.md](docs/lecture/바이브코딩-시작하기.md) | 입문자용 8단계 가이드 |
| [docs/inbox/README.md](docs/inbox/README.md) | 파일 요청·업로드 안내 |

---

## 화면

| 경로 | 내용 |
|---|---|
| `/` | 수익률 대시보드 — KPI · 적자 딜 · 채널별 · 제품별 · 주별 추이 |
| `/deals` | 제품·딜별 수익률 + 채널×제품 히트맵 + 손익분기 역산 |
| `/products` | 제품 DB — 계획 마진율 vs 채널별 실측 |
| `/channels` | 채널 관리 — 수수료율·물류비·정산기준 |
| `/mapping` | 상품명 매핑 — 미매핑 큐와 자동 제안 |
| `/upload` | 엑셀 업로드 → 매핑 → 손익 계산 |
| `/export` | 결과 엑셀 6시트 내려받기 |

---

## 계산식

```
순매출   = 판매금액 (취소면 0)
수수료   = 실적값 우선, 없으면 순매출 × 채널 요율
원가     = Σ(딜 구성 SKU × 판매일 기준 매입가) × 판매수량
물류비   = 채널 배송비 모델 × 판매수량
기여이익 = 순매출 − 수수료 − 자사부담할인 − 원가 − 물류비
마진율   = 기여이익 ÷ 순매출
```

**원가 = 매입가 × 구성수량**입니다. 오픈마켓 묶음 상품은 구성수량이 24·30·1000처럼 커서,
빼먹으면 원가가 수백 배 작아지고 전 제품이 흑자로 보입니다. 가장 흔한 실수입니다.

---

## 구성

| 구성요소 | 상태 | 위치 |
|---|:-:|---|
| 데이터 모델 | ✅ | `app/models.py` |
| 손익 계산 엔진 | ✅ | `app/services/calc.py` |
| 상품명 매핑 | ✅ | `app/services/mapping.py` |
| 엑셀 적재 (인풋 3장) | ✅ | `app/services/ingest.py` |
| 결과 엑셀 내보내기 | ✅ | `app/services/export.py` |
| 리포트 집계 | ✅ | `app/services/report.py` |
| 웹 화면 6개 | ✅ | `app/web.py`, `app/templates/` |
| 테스트 64건 | ✅ | `tests/` |
| 반품·쿠폰 · 정산 대사 | ❌ | P1 |

```bash
.venv/bin/python -m pytest -q
```

빈 DB에 엑셀 3장만 넣어도 정답지와 같은 숫자가 나오는지를 `tests/test_ingest.py` 가 지킵니다.

---

## 검수 기준

```
총매출 77,667,300 · 기여이익 5,954,482 · 마진율 7.67% · 주문 3,707건
미매핑 7건 / 214,300원 · 적자 딜 3건
```
